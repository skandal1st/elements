# =============================================================================
# Elements Platform - Production конфигурация для VDS
# =============================================================================
# Использование:
#   docker-compose -f docker-compose.prod.yml up -d
#   docker-compose -f docker-compose.prod.yml logs -f
#   docker-compose -f docker-compose.prod.yml down
# =============================================================================

services:
  # ===========================================================================
  # Базы данных и инфраструктура
  # ===========================================================================

  postgres:
    image: postgres:14-alpine
    container_name: elements-postgres
    restart: always
    env_file:
      - .env.production
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backups:/backups
      - ./scripts/init-db.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 30s
      timeout: 10s
      retries: 5
    networks:
      - backend

  redis:
    image: redis:7-alpine
    container_name: elements-redis
    restart: always
    env_file:
      - .env.production
    command: redis-server --requirepass ${REDIS_PASSWORD}
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "--raw", "incr", "ping"]
      interval: 30s
      timeout: 10s
      retries: 5
    networks:
      - backend

  qdrant:
    image: qdrant/qdrant:latest
    container_name: elements-qdrant
    restart: always
    volumes:
      - qdrant_data:/qdrant/storage
    # Образ Qdrant не содержит wget/curl — healthcheck отключён.
    # Backend зависит от service_started (контейнер запущен).
    networks:
      - backend

  # ===========================================================================
  # Elements Platform Backend (Python/FastAPI)
  # ===========================================================================

  backend:
    build:
      context: .
      dockerfile: ./backend/Dockerfile
    container_name: elements-backend
    restart: always
    env_file:
      - .env.production
    environment:
      # Database
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      
      # Auth
      - SECRET_KEY=${JWT_SECRET}
      - ACCESS_TOKEN_EXPIRE_MINUTES=10080
      
      # Modules
      - ENABLED_MODULES=hr,it,tasks
      
      # Redis
      - REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/0

      # Qdrant (Vector DB) — Knowledge Core stage 2
      - QDRANT_URL=${QDRANT_URL:-http://qdrant:6333}
      - QDRANT_COLLECTION=${QDRANT_COLLECTION:-knowledge_articles_v1}
      
      # Seed admin
      - SEED_ADMIN_ENABLED=${SEED_ADMIN_ENABLED:-true}
      - SEED_ADMIN_EMAIL=${SEED_ADMIN_EMAIL}
      - SEED_ADMIN_PASSWORD=${SEED_ADMIN_PASSWORD}
      
      # CORS
      - CORS_ORIGINS=${CORS_ORIGINS}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      qdrant:
        condition: service_started
    volumes:
      - uploads_data:/app/uploads
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 60s
      timeout: 10s
      retries: 3
    networks:
      - backend
      - frontend

  # ===========================================================================
  # Frontend (React + Vite)
  # ===========================================================================

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
      network: host
      args:
        - VITE_API_BASE=${VITE_API_BASE:-/api}
    env_file:
      - .env.production
    container_name: elements-frontend
    restart: always
    depends_on:
      - backend
    networks:
      - frontend

  # ===========================================================================
  # Nginx - Reverse Proxy
  # ===========================================================================

  nginx:
    image: nginx:alpine
    container_name: elements-nginx
    restart: always
    env_file:
      - .env.production
    ports:
      - "${HTTP_PORT:-80}:80"
      - "${HTTPS_PORT:-443}:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - ./ssl:/etc/nginx/ssl:ro
      - nginx_logs:/var/log/nginx
    depends_on:
      - backend
      - frontend
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - frontend

volumes:
  postgres_data:
    name: elements_postgres_data
  redis_data:
    name: elements_redis_data
  qdrant_data:
    name: elements_qdrant_data
  uploads_data:
    name: elements_uploads_data
  nginx_logs:
    name: elements_nginx_logs

networks:
  backend:
    name: elements_backend
    driver: bridge
  frontend:
    name: elements_frontend
    driver: bridge
